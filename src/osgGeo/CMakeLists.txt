IF   (DYNAMIC_OSGGEO)
    ADD_DEFINITIONS(-DOSGGEO_LIBRARY)
ELSE (DYNAMIC_OSGGEO)
    ADD_DEFINITIONS(-DOSGGEO_LIBRARY_STATIC)
ENDIF(DYNAMIC_OSGGEO)

SET(LIB_NAME osgEarth)

SET(HEADER_PATH ${OSGGEO_SOURCE_DIR}/include/${LIB_NAME})

add_library(LIB_NAME
	PolyLine.cpp)
